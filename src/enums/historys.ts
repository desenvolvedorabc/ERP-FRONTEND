export enum ActionTypes {
  INSERT = 'INSERT',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
  SIGN = 'SIGN',
  WITHDRAWAL = 'WITHDRAWAL',
  SETTLE = 'SETTLE',
}

export enum EntityType {
  CONTRACT = 'CONTRACT',
}
